require('dotenv').config();

const { test, expect } = require('@playwright/test');


test.describe.skip('Inspect/Review Doc Action Transcribe feature', () => {
    test.skip('Validate Transcribe options', async ({ page }) => {

    });
    test.skip('Validate Transcribe actions', async ({ page }) => {

    });
    test.skip('Validate Transcribe billing', async ({ page }) => {

    });
    test.skip('Validate Transcribe job submission', async ({ page }) => {

    });
});