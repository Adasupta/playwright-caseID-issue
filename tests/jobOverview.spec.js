require('dotenv').config();

const { test, expect } = require('@playwright/test');


test.describe('Job Overview  functionality', () => {
    test.skip('Job Filter functionality', async ({ page }) => {
    });
    test.skip('Verify jobname sort functionality', async ({ page }) => {
    });
    test.skip('Verify download report', async ({ page }) => {
    });
    test.skip('Verify download completed with error report', async ({ page }) => {
    });
    test.skip('Verify job details on job popup', async ({ page }) => {
    });
    test.skip('Verify job status', async ({ page }) => {
    });
    test.skip('Verify job delete functionality', async ({ page }) => {
    });
    test.skip('Verify job retry functionality', async ({ page }) => {
    });
    test.skip('Verify job cancel functionality', async ({ page }) => {
    });
    test.skip('Verify job pause functionality', async ({ page }) => {
    });
    test.skip('Verify job resume functionality', async ({ page }) => {
    });
    test.skip('Verify job progress bar functionality', async ({ page }) => {
    });
    test.skip('Verify pagination functionality', async ({ page }) => {
    });
    test.skip('Verify refresh functionality', async ({ page }) => {
    });


});