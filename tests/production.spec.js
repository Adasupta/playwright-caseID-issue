require('dotenv').config();

const { test, expect } = require('@playwright/test');





test.describe.skip('Production List validations', () => {

    test('create production', async ({ page }) => {

    });

    test('edit production', async ({ page }) => {
    });

    test('delete production', async ({ page }) => {
    });

});