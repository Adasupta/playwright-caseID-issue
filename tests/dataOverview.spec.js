require('dotenv').config();

const { test, expect } = require('@playwright/test');
const { ClientPage } = require('../functions/client');

test.describe('Data Overview Progress Icons functionality', () => {
    test.skip('Verify Collect Progress Icon and validate', async ({ page }) => {
    });
    test.skip('Verify user is able to download the collection report', async ({ page }) => {
    });
    test.skip('Verify Process Progress Icon and validate', async ({ page }) => {
    });
    test.skip('Verify user is able to download the processing report', async ({ page }) => {
    });
    test.skip('Verify Analyze Progress Icon and validate', async ({ page }) => {
    });
    test.skip('Verify Review Progress Icon and validate', async ({ page }) => {
    });
    test.skip('Verify Production Progress Icon and validate', async ({ page }) => {
    });

});

test.describe('Data Overview Data Information', () => {
    test.skip('Verify and validate all fields of Data information', async ({ page }) => {
    });
});

