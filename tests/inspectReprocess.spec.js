require('dotenv').config();

const { test, expect } = require('@playwright/test');


test.describe('Inspect Doc Action Reprocess feature', () => {
    test.skip('Validate Reprocess and submit job', async ({ page }) => {

    });
    
});